<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Audit Log - Pub's Lounge</title>
    <link rel="stylesheet" href="/dashboard/static/css/dashboard.css">
    <style>
        .nav-section-title {
            padding: 8px 16px;
            font-size: 11px;
            text-transform: uppercase;
            color: var(--text-muted);
            font-weight: 600;
            letter-spacing: 0.5px;
            margin-top: 8px;
        }
        .action-badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 4px;
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
        }
        .action-badge.create { background: rgba(35, 165, 89, 0.2); color: var(--accent-success); }
        .action-badge.update { background: rgba(59, 130, 246, 0.2); color: #3b82f6; }
        .action-badge.delete { background: rgba(220, 53, 69, 0.2); color: var(--accent-danger); }
        .action-badge.generate { background: rgba(168, 85, 247, 0.2); color: #a855f7; }
        .action-badge.login { background: rgba(234, 179, 8, 0.2); color: #eab308; }
        .details-cell {
            max-width: 300px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            font-size: 12px;
            color: var(--text-muted);
        }
        .details-cell:hover {
            white-space: normal;
            word-break: break-word;
        }
    </style>
</head>
<body>
    <div class="app-container">
        <aside class="sidebar">
            <div class="sidebar-header">
                <span class="logo">🍺</span>
                <h1>Pub's Lounge</h1>
            </div>
            <nav class="sidebar-nav">
                <a href="/dashboard" class="nav-item"><span class="nav-icon">📊</span><span>Dashboard</span></a>
                
                <div class="nav-section-title">🎮 Steam</div>
                <a href="/dashboard/games" class="nav-item"><span class="nav-icon">🎮</span><span>Games</span></a>
                <a href="/dashboard/accounts" class="nav-item"><span class="nav-icon">👤</span><span>Accounts</span></a>
                <a href="/dashboard/tokens" class="nav-item"><span class="nav-icon">🎫</span><span>Tokens</span></a>
                <a href="/dashboard/transcripts" class="nav-item"><span class="nav-icon">📜</span><span>Transcripts</span></a>
                <a href="/dashboard/activations" class="nav-item"><span class="nav-icon">✅</span><span>Activations</span></a>
                <a href="/dashboard/generate" class="nav-item"><span class="nav-icon">⚡</span><span>Manual Generate</span></a>
                
                <div class="nav-divider"></div>
                
                <div class="nav-section-title">🎯 Ubisoft</div>
                <a href="/dashboard/ubisoft-games" class="nav-item"><span class="nav-icon">🎮</span><span>Games</span></a>
                <a href="/dashboard/ubisoft-accounts" class="nav-item"><span class="nav-icon">👤</span><span>Accounts</span></a>
                <a href="/dashboard/ubisoft-tokens" class="nav-item"><span class="nav-icon">🎫</span><span>Tokens</span></a>
                <a href="/dashboard/ubisoft-transcripts" class="nav-item"><span class="nav-icon">📜</span><span>Transcripts</span></a>
                <a href="/dashboard/ubisoft-activations" class="nav-item"><span class="nav-icon">✅</span><span>Activations</span></a>
                <a href="/dashboard/ubisoft-generate" class="nav-item"><span class="nav-icon">⚡</span><span>Manual Generate</span></a>
                
                <div class="nav-divider"></div>
                
                <div class="nav-section-title">📈 Analytics</div>
                <a href="/dashboard/analytics" class="nav-item"><span class="nav-icon">📈</span><span>Analytics</span></a>
                
                <div class="nav-divider"></div>
                
                <div class="nav-section-title">⚙️ Admin</div>
                <a href="/dashboard/cooldowns" class="nav-item"><span class="nav-icon">⏱️</span><span>Cooldowns</span></a>
                <a href="/dashboard/audit-log" class="nav-item active"><span class="nav-icon">📋</span><span>Audit Log</span></a>
            </nav>
            <% if (user) { %>
            <div class="sidebar-footer">
                <div class="user-info">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="" class="user-avatar" onerror="this.style.display='none'">
                    <div class="user-details">
                        <span class="user-name"><%= user.username %></span>
                        <span class="user-role">Staff</span>
                    </div>
                </div>
                <a href="/dashboard/logout" class="logout-btn">Logout</a>
            </div>
            <% } %>
        </aside>
        
        <main class="main-content">
            <header class="content-header">
                <h2>📋 Audit Log</h2>
            </header>
            
            <div class="content-body">
                <% if (logs && logs.length > 0) { %>
                <div class="table-container">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Action</th>
                                <th>Category</th>
                                <th>Target</th>
                                <th>User</th>
                                <th>Details</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% logs.forEach(log => { %>
                            <tr>
                                <td><%= new Date(log.created_at).toLocaleString() %></td>
                                <td>
                                    <span class="action-badge <%= log.action %>"><%= log.action %></span>
                                </td>
                                <td><%= log.category || '-' %></td>
                                <td><%= log.target_name || log.target_id || '-' %></td>
                                <td><%= log.username || 'System' %></td>
                                <td class="details-cell" title="<%= log.details || '' %>">
                                    <%= log.details || '-' %>
                                </td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
                <% } else { %>
                <div class="empty-state">
                    <p>No audit logs recorded yet.</p>
                </div>
                <% } %>
            </div>
        </main>
    </div>
</body>
</html>
