<%- include('../partials/header') %>

<%
// Helper function to mask email for privacy
function maskEmail(email) {
    if (!email) return '-';
    const parts = email.split('@');
    if (parts.length !== 2) return '***';
    const local = parts[0];
    const domain = parts[1];
    const domainParts = domain.split('.');
    
    // Mask local part: show first char + ***
    const maskedLocal = local.charAt(0) + '***';
    
    // Mask domain: show first char + *** + .extension
    const ext = domainParts.pop();
    const maskedDomain = domainParts[0]?.charAt(0) + '***.' + ext;
    
    return maskedLocal + '@' + maskedDomain;
}
%>

<div class="page-header">
    <h1 class="page-title">
        <span class="icon">👤</span>
        Ubisoft Accounts
    </h1>
</div>

<div class="card">
    <div class="card-header">
        <span class="card-title">Add Account</span>
    </div>
    <form method="POST" action="/dashboard/ubisoft/accounts/add">
        <div class="form-row">
            <div class="form-group">
                <input type="text" name="account_name" class="form-control" placeholder="Account name" required>
            </div>
            <div class="form-group">
                <input type="email" name="email" class="form-control" placeholder="Email" required>
            </div>
            <div class="form-group">
                <input type="password" name="password" class="form-control" placeholder="Password" required>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary">➕ Add</button>
            </div>
        </div>
    </form>
</div>

<div class="card">
    <% if (accounts && accounts.length > 0) { %>
    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>Account Name</th>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% accounts.forEach(acc => { %>
                <tr>
                    <td><strong><%= acc.account_name %></strong></td>
                    <td><code><%= maskEmail(acc.email) %></code></td>
                    <td>
                        <span class="badge badge-<%= acc.enabled ? 'success' : 'danger' %>">
                            <%= acc.enabled ? 'Active' : 'Disabled' %>
                        </span>
                    </td>
                    <td>
                        <form method="POST" action="/dashboard/ubisoft/accounts/<%= acc.id %>/delete" data-confirm="Delete this account?" style="display:inline;">
                            <button type="submit" class="btn btn-danger btn-sm">🗑️</button>
                        </form>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    <% } else { %>
    <div class="empty-state">
        <div class="icon">👤</div>
        <p>No accounts configured.</p>
    </div>
    <% } %>
</div>

<%- include('../partials/footer') %>
