<%# Layout Helper - Include at top of each template %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Pub's Lounge</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Tahoma, sans-serif; background: #0a0a0f; color: #fff; min-height: 100vh; }
        .dashboard-container { display: flex; min-height: 100vh; }
        .sidebar { width: 260px; background: linear-gradient(180deg, #12121a 0%, #0d0d14 100%); border-right: 1px solid #1a1a2e; display: flex; flex-direction: column; position: fixed; height: 100vh; overflow-y: auto; }
        .sidebar-header { padding: 20px; border-bottom: 1px solid #1a1a2e; display: flex; align-items: center; gap: 12px; }
        .sidebar-header .logo { font-size: 2rem; }
        .sidebar-header h1 { font-size: 1.1rem; color: #e94560; }
        .sidebar-nav { flex: 1; padding: 15px 0; }
        .nav-section { padding: 8px 20px; font-size: 11px; text-transform: uppercase; color: #666; font-weight: 600; letter-spacing: 0.5px; margin-top: 10px; }
        .nav-item { display: flex; align-items: center; gap: 12px; padding: 12px 20px; color: #888; text-decoration: none; transition: all 0.2s; border-left: 3px solid transparent; }
        .nav-item:hover { background: rgba(233, 69, 96, 0.1); color: #fff; }
        .nav-item.active { background: rgba(233, 69, 96, 0.15); color: #e94560; border-left-color: #e94560; }
        .nav-icon { font-size: 1.1rem; width: 24px; text-align: center; }
        .nav-divider { height: 1px; background: #1a1a2e; margin: 10px 20px; }
        .sidebar-footer { padding: 15px 20px; border-top: 1px solid #1a1a2e; }
        .user-info { display: flex; align-items: center; gap: 10px; margin-bottom: 10px; }
        .user-avatar { width: 32px; height: 32px; background: #e94560; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 0.9rem; }
        .user-name { font-weight: 600; font-size: 0.9rem; }
        .user-role { font-size: 0.75rem; color: #888; text-transform: capitalize; }
        .logout-btn { display: block; text-align: center; padding: 8px; background: rgba(220, 53, 69, 0.2); color: #dc3545; text-decoration: none; border-radius: 5px; font-size: 0.85rem; }
        .logout-btn:hover { background: rgba(220, 53, 69, 0.3); }
        .main-content { flex: 1; margin-left: 260px; min-height: 100vh; }
        .content-header { padding: 20px 30px; background: #12121a; border-bottom: 1px solid #1a1a2e; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 10px; }
        .content-header h2 { font-size: 1.4rem; }
        .content-body { padding: 25px 30px; }
        .card { background: #16213e; border: 1px solid #0f3460; border-radius: 10px; padding: 20px; margin-bottom: 20px; }
        .card-title { font-size: 1.1rem; margin-bottom: 15px; padding-bottom: 10px; border-bottom: 1px solid #0f3460; display: flex; justify-content: space-between; align-items: center; }
        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 20px; margin-bottom: 25px; }
        .stat-card { background: linear-gradient(135deg, #16213e 0%, #0f3460 100%); border: 1px solid #0f3460; border-radius: 10px; padding: 20px; text-align: center; }
        .stat-value { font-size: 2.2rem; font-weight: bold; color: #e94560; }
        .stat-label { color: #888; font-size: 0.9rem; margin-top: 5px; }
        .table-responsive { overflow-x: auto; }
        .table { width: 100%; border-collapse: collapse; }
        .table th, .table td { padding: 12px 15px; text-align: left; border-bottom: 1px solid #0f3460; }
        .table th { background: #0f3460; font-weight: 600; font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.5px; }
        .table tr:hover { background: rgba(233, 69, 96, 0.05); }
        .btn { padding: 10px 20px; border: none; border-radius: 6px; cursor: pointer; font-size: 0.9rem; text-decoration: none; display: inline-flex; align-items: center; gap: 6px; transition: all 0.2s; }
        .btn-primary { background: linear-gradient(135deg, #e94560 0%, #c73e54 100%); color: #fff; }
        .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 4px 12px rgba(233, 69, 96, 0.3); }
        .btn-secondary { background: #0f3460; color: #fff; }
        .btn-secondary:hover { background: #1a4a7a; }
        .btn-danger { background: #dc3545; color: #fff; }
        .btn-success { background: #28a745; color: #fff; }
        .btn-sm { padding: 6px 12px; font-size: 0.8rem; }
        .form-group { margin-bottom: 15px; }
        .form-group label { display: block; margin-bottom: 6px; color: #888; font-size: 0.9rem; }
        .form-control { width: 100%; padding: 10px 12px; border: 1px solid #0f3460; border-radius: 6px; background: #1a1a2e; color: #fff; font-size: 0.95rem; }
        .form-control:focus { outline: none; border-color: #e94560; }
        .form-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; }
        select.form-control { cursor: pointer; }
        .badge { padding: 4px 10px; border-radius: 4px; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; display: inline-block; }
        .badge-steam { background: #1b2838; color: #66c0f4; }
        .badge-ubisoft { background: #0070ff; color: #fff; }
        .badge-ea { background: #ff4747; color: #fff; }
        .badge-open { background: #ffc107; color: #000; }
        .badge-pending { background: #17a2b8; color: #fff; }
        .badge-processing { background: #6f42c1; color: #fff; }
        .badge-completed { background: #28a745; color: #fff; }
        .badge-closed { background: #6c757d; color: #fff; }
        .badge-failed { background: #dc3545; color: #fff; }
        .badge-success { background: #28a745; color: #fff; }
        .badge-warning { background: #ffc107; color: #000; }
        .badge-high { background: #ff6b6b; color: #fff; }
        .badge-normal { background: #4a5568; color: #fff; }
        .alert { padding: 12px 15px; border-radius: 6px; margin-bottom: 15px; }
        .alert-success { background: rgba(40, 167, 69, 0.2); border: 1px solid #28a745; color: #28a745; }
        .alert-error { background: rgba(220, 53, 69, 0.2); border: 1px solid #dc3545; color: #dc3545; }
        .alert-warning { background: rgba(255, 193, 7, 0.2); border: 1px solid #ffc107; color: #ffc107; }
        .modal { display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); z-index: 1000; justify-content: center; align-items: center; }
        .modal.active { display: flex; }
        .modal-content { background: #16213e; border: 1px solid #0f3460; border-radius: 10px; padding: 25px; width: 90%; max-width: 500px; max-height: 90vh; overflow-y: auto; }
        .modal-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; padding-bottom: 15px; border-bottom: 1px solid #0f3460; }
        .modal-close { background: none; border: none; color: #888; font-size: 1.5rem; cursor: pointer; }
        .modal-close:hover { color: #fff; }
        .tabs { display: flex; gap: 5px; margin-bottom: 20px; flex-wrap: wrap; }
        .tab { padding: 10px 20px; background: #1a1a2e; border: 1px solid #0f3460; border-radius: 6px; color: #888; text-decoration: none; transition: all 0.2s; }
        .tab:hover { background: #0f3460; color: #fff; }
        .tab.active { background: #e94560; border-color: #e94560; color: #fff; }
        .filter-group { display: flex; gap: 10px; align-items: center; }
        code { background: #0f3460; padding: 2px 6px; border-radius: 3px; font-family: monospace; }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <div class="sidebar">
            <div class="sidebar-header">
                <span class="logo">🍺</span>
                <h1>Pub's Lounge</h1>
            </div>
            <nav class="sidebar-nav">
                <% const isStaff = user && ['admin', 'staff'].includes(user.role); %>
                <% const isAdmin = user && user.role === 'admin'; %>
                <% const isBartender = user && user.role === 'bartender'; %>
                
                <% if (isStaff) { %>
                <a href="/dashboard" class="nav-item <%= path === '/' ? 'active' : '' %>"><span class="nav-icon">🏠</span> Dashboard</a>
                <a href="/dashboard/tickets" class="nav-item <%= path.startsWith('/tickets') ? 'active' : '' %>"><span class="nav-icon">🎫</span> Live Tickets</a>
                
                <div class="nav-section">🎮 Steam</div>
                <a href="/dashboard/steam/games" class="nav-item <%= path === '/steam/games' ? 'active' : '' %>"><span class="nav-icon">🎲</span> Games</a>
                <a href="/dashboard/steam/accounts" class="nav-item <%= path === '/steam/accounts' ? 'active' : '' %>"><span class="nav-icon">👤</span> Accounts</a>
                <a href="/dashboard/steam/tokens" class="nav-item <%= path === '/steam/tokens' ? 'active' : '' %>"><span class="nav-icon">🎟️</span> Tokens</a>
                <a href="/dashboard/steam/transcripts" class="nav-item <%= path === '/steam/transcripts' ? 'active' : '' %>"><span class="nav-icon">💬</span> Transcripts</a>
                <a href="/dashboard/steam/activations" class="nav-item <%= path === '/steam/activations' ? 'active' : '' %>"><span class="nav-icon">✅</span> Activations</a>
                <a href="/dashboard/steam/generate" class="nav-item <%= path === '/steam/generate' ? 'active' : '' %>"><span class="nav-icon">⚡</span> Generate</a>
                
                <div class="nav-section">🎯 Ubisoft</div>
                <a href="/dashboard/ubisoft/games" class="nav-item <%= path === '/ubisoft/games' ? 'active' : '' %>"><span class="nav-icon">🎲</span> Games</a>
                <a href="/dashboard/ubisoft/accounts" class="nav-item <%= path === '/ubisoft/accounts' ? 'active' : '' %>"><span class="nav-icon">👤</span> Accounts</a>
                <a href="/dashboard/ubisoft/tokens" class="nav-item <%= path === '/ubisoft/tokens' ? 'active' : '' %>"><span class="nav-icon">🎟️</span> Tokens</a>
                <a href="/dashboard/ubisoft/transcripts" class="nav-item <%= path === '/ubisoft/transcripts' ? 'active' : '' %>"><span class="nav-icon">💬</span> Transcripts</a>
                <a href="/dashboard/ubisoft/activations" class="nav-item <%= path === '/ubisoft/activations' ? 'active' : '' %>"><span class="nav-icon">✅</span> Activations</a>
                <a href="/dashboard/ubisoft/generate" class="nav-item <%= path === '/ubisoft/generate' ? 'active' : '' %>"><span class="nav-icon">⚡</span> Generate</a>
                
                <div class="nav-section">🔴 EA</div>
                <a href="/dashboard/ea/games" class="nav-item <%= path === '/ea/games' ? 'active' : '' %>"><span class="nav-icon">🎲</span> Games</a>
                <a href="/dashboard/ea/accounts" class="nav-item <%= path === '/ea/accounts' ? 'active' : '' %>"><span class="nav-icon">👤</span> Accounts</a>
                <a href="/dashboard/ea/transcripts" class="nav-item <%= path === '/ea/transcripts' ? 'active' : '' %>"><span class="nav-icon">💬</span> Transcripts</a>
                <a href="/dashboard/ea/activations" class="nav-item <%= path === '/ea/activations' ? 'active' : '' %>"><span class="nav-icon">✅</span> Activations</a>
                <a href="/dashboard/ea/queue" class="nav-item <%= path === '/ea/queue' ? 'active' : '' %>"><span class="nav-icon">📋</span> Queue</a>
                
                <div class="nav-section">📈 Reports</div>
                <a href="/dashboard/analytics" class="nav-item <%= path === '/analytics' ? 'active' : '' %>"><span class="nav-icon">📊</span> Analytics</a>
                <a href="/dashboard/staff-activity" class="nav-item <%= path === '/staff-activity' ? 'active' : '' %>"><span class="nav-icon">👥</span> Staff Activity</a>
                <% if (isAdmin) { %><a href="/dashboard/audit-logs" class="nav-item <%= path === '/audit-logs' ? 'active' : '' %>"><span class="nav-icon">📋</span> Audit Logs</a><% } %>
                
                <div class="nav-section">⚙️ Settings</div>
                <a href="/dashboard/cooldowns" class="nav-item <%= path === '/cooldowns' ? 'active' : '' %>"><span class="nav-icon">⏰</span> Cooldowns</a>
                <a href="/dashboard/macros" class="nav-item <%= path === '/macros' ? 'active' : '' %>"><span class="nav-icon">⚡</span> Macros</a>
                <% if (isAdmin) { %><a href="/dashboard/users" class="nav-item <%= path === '/users' ? 'active' : '' %>"><span class="nav-icon">👥</span> Users</a><% } %>
                <% } %>
                
                <% if (isBartender) { %>
                <div class="nav-section">🎮 Steam</div>
                <a href="/dashboard/steam/generate" class="nav-item <%= path === '/steam/generate' ? 'active' : '' %>"><span class="nav-icon">⚡</span> Generate Tokens</a>
                <% } %>
                
                <div class="nav-divider"></div>
                <a href="/dashboard/change-password" class="nav-item <%= path === '/change-password' ? 'active' : '' %>"><span class="nav-icon">🔑</span> Change Password</a>
            </nav>
            <div class="sidebar-footer">
                <div class="user-info">
                    <div class="user-avatar">👤</div>
                    <div>
                        <div class="user-name"><%= user?.username || 'Guest' %></div>
                        <div class="user-role"><%= user?.role || 'unknown' %></div>
                    </div>
                </div>
                <a href="/dashboard/logout" class="logout-btn">🚪 Logout</a>
            </div>
        </div>
        <div class="main-content">
