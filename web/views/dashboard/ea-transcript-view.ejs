<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Pub's Lounge</title>
    <link rel="stylesheet" href="/dashboard/static/css/dashboard.css">
    <style>
        .nav-section-title { padding: 12px 20px 6px; font-size: 11px; text-transform: uppercase; color: #666; font-weight: 600; }
        .card { background: #16213e; border: 1px solid #0f3460; border-radius: 10px; padding: 20px; margin-bottom: 20px; }
        .card-title { font-size: 1.2rem; margin-bottom: 15px; border-bottom: 1px solid #0f3460; padding-bottom: 10px; }
        .info-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; margin-bottom: 20px; }
        .info-item { background: #0f3460; padding: 12px; border-radius: 5px; }
        .info-label { color: #888; font-size: 0.8rem; margin-bottom: 5px; }
        .info-value { font-size: 1rem; }
        .messages-container { max-height: 600px; overflow-y: auto; }
        .message { padding: 12px; border-bottom: 1px solid #0f3460; }
        .message:last-child { border-bottom: none; }
        .message-header { display: flex; justify-content: space-between; margin-bottom: 5px; }
        .message-author { font-weight: bold; color: #e94560; }
        .message-time { color: #666; font-size: 0.8rem; }
        .message-content { color: #ccc; white-space: pre-wrap; word-break: break-word; }
        .message-bot { background: rgba(88, 101, 242, 0.1); }
        .btn { padding: 8px 16px; border: none; border-radius: 5px; cursor: pointer; text-decoration: none; display: inline-block; }
        .btn-secondary { background: #0f3460; color: #fff; }
        .embed-preview { background: #2f3136; border-left: 4px solid #e94560; padding: 10px; margin-top: 5px; border-radius: 0 5px 5px 0; }
        .embed-title { font-weight: bold; margin-bottom: 5px; }
        .embed-description { color: #b9bbbe; font-size: 0.9rem; }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <aside class="sidebar">
            <div class="sidebar-header"><span class="logo">🍺</span><h1>Pub's Lounge</h1></div>
            <nav class="sidebar-nav">
                <a href="/dashboard" class="nav-item"><span class="nav-icon">📊</span><span>Dashboard</span></a>
                <a href="/dashboard/games" class="nav-item"><span class="nav-icon">🎮</span><span>Games</span></a>
                <a href="/dashboard/accounts" class="nav-item"><span class="nav-icon">👤</span><span>Accounts</span></a>
                <a href="/dashboard/tokens" class="nav-item"><span class="nav-icon">🎫</span><span>Tokens</span></a>
                <a href="/dashboard/transcripts" class="nav-item"><span class="nav-icon">📜</span><span>Transcripts</span></a>
                <div class="nav-divider"></div>
                <div class="nav-section-title">🔴 EA</div>
                <a href="/dashboard/ea" class="nav-item"><span class="nav-icon">🎮</span><span>EA Games</span></a>
                <a href="/dashboard/ea/tickets" class="nav-item"><span class="nav-icon">🎫</span><span>EA Tickets</span></a>
                <a href="/dashboard/ea/cooldowns" class="nav-item"><span class="nav-icon">⏱️</span><span>EA Cooldowns</span></a>
                <a href="/dashboard/ea/transcripts" class="nav-item active"><span class="nav-icon">📜</span><span>EA Transcripts</span></a>
                <div class="nav-divider"></div>
                <a href="/dashboard/analytics" class="nav-item"><span class="nav-icon">📈</span><span>Analytics</span></a>
                <a href="/dashboard/audit-logs" class="nav-item"><span class="nav-icon">📋</span><span>Audit Logs</span></a>
            </nav>
            <% if (user) { %>
            <div class="sidebar-footer">
                <div class="user-info">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="" class="user-avatar" onerror="this.style.display='none'">
                    <div class="user-details"><span class="user-name"><%= user.username %></span><span class="user-role">Staff</span></div>
                </div>
                <a href="/dashboard/logout" class="logout-btn">Logout</a>
            </div>
            <% } %>
        </aside>
        
        <main class="main-content">
            <header class="content-header">
                <h2><%= title %></h2>
                <a href="/dashboard/ea/transcripts" class="btn btn-secondary">← Back to Transcripts</a>
            </header>
            
            <div class="content-body">
                <div class="card">
                    <div class="card-title">Ticket Information</div>
                    <div class="info-grid">
                        <div class="info-item"><div class="info-label">Ticket ID</div><div class="info-value"><code><%= transcript.ticket_id %></code></div></div>
                        <div class="info-item"><div class="info-label">User</div><div class="info-value"><%= transcript.username || transcript.user_id %></div></div>
                        <div class="info-item"><div class="info-label">User ID</div><div class="info-value"><code><%= transcript.user_id %></code></div></div>
                        <div class="info-item"><div class="info-label">Game</div><div class="info-value"><%= transcript.game_name || 'N/A' %></div></div>
                        <div class="info-item"><div class="info-label">Created</div><div class="info-value"><%= new Date(transcript.created_at).toLocaleString() %></div></div>
                        <div class="info-item"><div class="info-label">Messages</div><div class="info-value"><%= messages.length %></div></div>
                    </div>
                </div>
                
                <div class="card">
                    <div class="card-title">Conversation</div>
                    <div class="messages-container">
                        <% if (messages && messages.length > 0) { %>
                            <% for (const msg of messages) { 
                                const isBot = msg.author && (msg.author.includes('Bot') || msg.author.includes('[BOT]'));
                            %>
                                <div class="message <%= isBot ? 'message-bot' : '' %>">
                                    <div class="message-header">
                                        <span class="message-author"><%= msg.author || 'Unknown' %></span>
                                        <span class="message-time"><%= msg.timestamp ? new Date(msg.timestamp).toLocaleString() : '' %></span>
                                    </div>
                                    <% if (msg.content) { %><div class="message-content"><%= msg.content %></div><% } %>
                                    <% if (msg.embeds && msg.embeds.length > 0) { %>
                                        <% for (const embed of msg.embeds) { %>
                                            <div class="embed-preview">
                                                <% if (embed.title) { %><div class="embed-title"><%= embed.title %></div><% } %>
                                                <% if (embed.description) { %><div class="embed-description"><%= embed.description %></div><% } %>
                                            </div>
                                        <% } %>
                                    <% } %>
                                </div>
                            <% } %>
                        <% } else { %>
                            <div style="text-align: center; padding: 20px; color: #666;">No messages in transcript.</div>
                        <% } %>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
