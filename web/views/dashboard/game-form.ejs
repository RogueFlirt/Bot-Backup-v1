<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Pub's Lounge</title>
    <link rel="stylesheet" href="/dashboard/static/css/dashboard.css">
</head>
<body>
    <div class="app-container">
        <aside class="sidebar">
            <div class="sidebar-header">
                <span class="logo">🍺</span>
                <h1>Pub's Lounge</h1>
            </div>
            <nav class="sidebar-nav">
                <a href="/dashboard" class="nav-item"><span class="nav-icon">📊</span><span>Dashboard</span></a>
                <a href="/dashboard/games" class="nav-item active"><span class="nav-icon">🎮</span><span>Games</span></a>
                <a href="/dashboard/accounts" class="nav-item"><span class="nav-icon">👤</span><span>Accounts</span></a>
                <a href="/dashboard/tokens" class="nav-item"><span class="nav-icon">🎫</span><span>Tokens</span></a>
                <a href="/dashboard/transcripts" class="nav-item"><span class="nav-icon">📜</span><span>Transcripts</span></a>
                <div class="nav-divider"></div>
                <div class="nav-section-title" style="padding: 8px 16px; font-size: 11px; text-transform: uppercase; color: var(--text-muted); font-weight: 600; letter-spacing: 0.5px;">🎯 Ubisoft</div>
                <a href="/dashboard/ubisoft-games" class="nav-item">
                    <span class="nav-icon">🎮</span>
                    <span>Ubisoft Games</span>
                </a>
                <a href="/dashboard/ubisoft-accounts" class="nav-item">
                    <span class="nav-icon">👤</span>
                    <span>Ubisoft Accounts</span>
                </a>
                <a href="/dashboard/ubisoft-tokens" class="nav-item">
                    <span class="nav-icon">🎫</span>
                    <span>Ubisoft Tokens</span>
                </a>
                <a href="/dashboard/ubisoft-activations" class="nav-item">
                    <span class="nav-icon">✅</span>
                    <span>Ubisoft Activations</span>
                </a>
                <div class="nav-divider"></div>
                <a href="/dashboard/generate" class="nav-item"><span class="nav-icon">⚡</span><span>Generate Token</span></a>
            </nav>
            <% if (user) { %>
            <div class="sidebar-footer">
                <div class="user-info">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="" class="user-avatar" onerror="this.style.display='none'">
                    <div class="user-details">
                        <span class="user-name"><%= user.username %></span>
                        <span class="user-role">Staff</span>
                    </div>
                </div>
                <a href="/dashboard/logout" class="logout-btn">Logout</a>
            </div>
            <% } %>
        </aside>
        
        <main class="main-content">
            <header class="content-header">
                <h2><%= title %></h2>
                <div class="header-actions">
                    <a href="/dashboard/games" class="btn btn-secondary">← Back to Games</a>
                </div>
            </header>
            
            <div class="content-body">
                <div class="card" style="max-width: 600px;">
                    <div class="card-header">
                        <h3><%= game ? '✏️ Edit Game' : '➕ Add New Game' %></h3>
                    </div>
                    <div class="card-body">
                        <form method="POST" action="<%= action %>">
                            <div class="form-group">
                                <label class="form-label">Game Name *</label>
                                <input type="text" name="game_name" class="form-input" required 
                                       value="<%= game ? game.game_name : '' %>" 
                                       placeholder="e.g. Black Myth Wukong">
                            </div>
                            
                            <div class="form-group">
                                <label class="form-label">Game ID (Slug)</label>
                                <input type="text" name="game_id" class="form-input" 
                                       value="<%= game ? game.game_id : '' %>" 
                                       placeholder="e.g. black-myth-wukong (auto-generated if empty)">
                                <small style="color: var(--text-muted);">Used internally for identification</small>
                            </div>
                            
                            <div class="form-group">
                                <label class="form-label">Size (GB)</label>
                                <input type="number" name="size_gb" class="form-input" step="0.01"
                                       value="<%= game ? game.size_gb : '' %>" 
                                       placeholder="e.g. 140.5">
                            </div>
                            
                            <div class="form-group">
                                <label class="form-label">Folder Name</label>
                                <input type="text" name="folder_name" class="form-input" 
                                       value="<%= game ? game.folder_name : '' %>" 
                                       placeholder="e.g. Black Myth Wukong (exact folder name on accounts)">
                                <small style="color: var(--text-muted);">Must match exact folder name on shared accounts</small>
                            </div>
                            
                            <div class="form-group">
                                <label class="form-label">Cover Image URL</label>
                                <input type="text" name="cover_url" class="form-input" 
                                       value="<%= game ? game.cover_url : '' %>" 
                                       placeholder="https://...">
                            </div>
                            
                            <div class="form-group">
                                <label class="form-label">Demand Type</label>
                                <select name="demand_type" class="form-select">
                                    <option value="normal" <%= game && game.demand_type === 'normal' ? 'selected' : '' %>>🟢 Normal</option>
                                    <option value="high" <%= game && game.demand_type === 'high' ? 'selected' : '' %>>🔥 High Demand</option>
                                </select>
                            </div>
                            
                            <div class="form-group">
                                <label class="form-label">Panel Visibility</label>
                                <div style="display: flex; flex-direction: column; gap: 12px; padding: 10px 0;">
                                    <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
                                        <input type="radio" name="panel_visibility" value="both" 
                                               <%= !game || (game.free_panel === 1 && game.hidden !== 1) ? 'checked' : '' %>
                                               style="width: 18px; height: 18px;">
                                        <span>🆓 Show on Both Panels (Free + Donator)</span>
                                    </label>
                                    <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
                                        <input type="radio" name="panel_visibility" value="donator" 
                                               <%= game && game.free_panel === 0 && game.hidden !== 1 ? 'checked' : '' %>
                                               style="width: 18px; height: 18px;">
                                        <span>💎 Donator Panel Only (Gold/Silver/Bronze)</span>
                                    </label>
                                    <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
                                        <input type="radio" name="panel_visibility" value="hidden" 
                                               <%= game && game.hidden === 1 ? 'checked' : '' %>
                                               style="width: 18px; height: 18px;">
                                        <span>👁️‍🗨️ Hidden (Not shown on any panel)</span>
                                    </label>
                                </div>
                                <small style="color: var(--text-muted);">
                                    Hidden games remain in the dashboard but won't appear on any user-facing panel
                                </small>
                            </div>
                            
                            <% if (!game) { %>
                            <div class="form-group" style="margin-top: 16px; padding: 16px; background: var(--bg-primary); border-radius: 8px; border: 1px solid var(--border-color);">
                                <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
                                    <input type="checkbox" name="create_tokens_all_accounts" value="1" 
                                           style="width: 18px; height: 18px;">
                                    <span>🎫 Create token slots for ALL existing accounts</span>
                                </label>
                                <small style="color: var(--text-muted); display: block; margin-top: 8px;">
                                    Automatically adds 1 token slot per account when game is created
                                </small>
                            </div>
                            <% } %>
                            
                            <div style="display: flex; gap: 12px; margin-top: 24px;">
                                <button type="submit" class="btn btn-primary">
                                    <%= game ? '💾 Save Changes' : '➕ Add Game' %>
                                </button>
                                <a href="/dashboard/games" class="btn btn-secondary">Cancel</a>
                            </div>
                        </form>
                        
                        <% if (game) { %>
                        <div style="margin-top: 32px; padding-top: 24px; border-top: 1px solid var(--border-color);">
                            <h4 style="color: var(--accent-danger); margin-bottom: 12px;">⚠️ Danger Zone</h4>
                            <p style="color: var(--text-muted); margin-bottom: 16px;">
                                Deleting this game will also delete all associated tokens. This action cannot be undone.
                            </p>
                            <form method="POST" action="/dashboard/games/<%= game.id %>/delete" 
                                  onsubmit="return confirm('Are you sure you want to delete <%= game.game_name %>?\n\nThis will also delete all tokens for this game!');">
                                <button type="submit" class="btn btn-danger">🗑️ Delete Game</button>
                            </form>
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
