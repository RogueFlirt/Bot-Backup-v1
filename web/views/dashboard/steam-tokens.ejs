<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Pub's Lounge</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', sans-serif; background: #0a0a0f; color: #fff; min-height: 100vh; }
        .dashboard-container { display: flex; min-height: 100vh; }
        .sidebar { width: 260px; background: linear-gradient(180deg, #12121a 0%, #0d0d14 100%); border-right: 1px solid #1a1a2e; position: fixed; height: 100vh; overflow-y: auto; }
        .sidebar-header { padding: 20px; border-bottom: 1px solid #1a1a2e; display: flex; align-items: center; gap: 12px; }
        .sidebar-header h1 { font-size: 1.1rem; color: #e94560; }
        .sidebar-nav { padding: 15px 0; }
        .nav-section { padding: 8px 20px; font-size: 11px; text-transform: uppercase; color: #666; font-weight: 600; margin-top: 10px; }
        .nav-item { display: flex; align-items: center; gap: 12px; padding: 12px 20px; color: #888; text-decoration: none; border-left: 3px solid transparent; }
        .nav-item:hover { background: rgba(233, 69, 96, 0.1); color: #fff; }
        .nav-item.active { background: rgba(233, 69, 96, 0.15); color: #e94560; border-left-color: #e94560; }
        .nav-divider { height: 1px; background: #1a1a2e; margin: 10px 20px; }
        .sidebar-footer { padding: 15px 20px; border-top: 1px solid #1a1a2e; position: absolute; bottom: 0; width: 100%; background: #0d0d14; }
        .logout-btn { display: block; text-align: center; padding: 8px; background: rgba(220, 53, 69, 0.2); color: #dc3545; text-decoration: none; border-radius: 5px; margin-top: 10px; }
        .main-content { flex: 1; margin-left: 260px; }
        .content-header { padding: 20px 30px; background: #12121a; border-bottom: 1px solid #1a1a2e; display: flex; justify-content: space-between; align-items: center; }
        .content-body { padding: 25px 30px; }
        .card { background: #16213e; border: 1px solid #0f3460; border-radius: 10px; padding: 20px; margin-bottom: 20px; }
        .card-title { font-size: 1rem; margin-bottom: 15px; padding-bottom: 10px; border-bottom: 1px solid #0f3460; }
        .stats-row { display: flex; gap: 15px; margin-bottom: 20px; flex-wrap: wrap; }
        .stat-box { background: #0f3460; padding: 15px 25px; border-radius: 8px; text-align: center; min-width: 120px; }
        .stat-box .value { font-size: 1.8rem; font-weight: bold; }
        .stat-box .label { font-size: 0.8rem; color: #888; }
        .stat-box.available .value { color: #28a745; }
        .stat-box.used .value { color: #ffc107; }
        .stat-box.reserved .value { color: #17a2b8; }
        .filter-bar { display: flex; gap: 15px; margin-bottom: 20px; align-items: center; flex-wrap: wrap; }
        .form-control { padding: 10px 12px; border: 1px solid #0f3460; border-radius: 6px; background: #1a1a2e; color: #fff; }
        .form-control:focus { outline: none; border-color: #e94560; }
        .table { width: 100%; border-collapse: collapse; }
        .table th, .table td { padding: 10px 12px; text-align: left; border-bottom: 1px solid #0f3460; font-size: 0.9rem; }
        .table th { background: #0f3460; font-weight: 600; font-size: 0.8rem; }
        .table tr:hover { background: rgba(233, 69, 96, 0.05); }
        .btn { padding: 8px 16px; border: none; border-radius: 5px; cursor: pointer; text-decoration: none; font-size: 0.85rem; }
        .btn-primary { background: #e94560; color: #fff; }
        .btn-secondary { background: #0f3460; color: #fff; }
        .btn-danger { background: #dc3545; color: #fff; }
        .btn-warning { background: #ffc107; color: #000; }
        .btn-sm { padding: 5px 10px; font-size: 0.75rem; }
        .badge { padding: 4px 8px; border-radius: 4px; font-size: 0.7rem; font-weight: 600; }
        .badge-available { background: #28a745; color: #fff; }
        .badge-used { background: #ffc107; color: #000; }
        .badge-reserved { background: #17a2b8; color: #fff; }
        .action-bar { display: flex; gap: 10px; flex-wrap: wrap; }
        .empty-state { text-align: center; padding: 40px; color: #666; }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <aside class="sidebar">
            <div class="sidebar-header"><span style="font-size:2rem">🍺</span><h1>Pub's Lounge</h1></div>
            <nav class="sidebar-nav">
                <a href="/dashboard" class="nav-item"><span>📊</span><span>Dashboard</span></a>
                <a href="/dashboard/tickets" class="nav-item"><span>🎫</span><span>Live Tickets</span></a>
                <div class="nav-section">🎮 Steam</div>
                <a href="/dashboard/steam/games" class="nav-item"><span>🎮</span><span>Games</span></a>
                <a href="/dashboard/steam/accounts" class="nav-item"><span>👤</span><span>Accounts</span></a>
                <a href="/dashboard/steam/tokens" class="nav-item active"><span>🎫</span><span>Tokens</span></a>
                <a href="/dashboard/steam/transcripts" class="nav-item"><span>📜</span><span>Transcripts</span></a>
                <a href="/dashboard/steam/activations" class="nav-item"><span>✅</span><span>Activations</span></a>
                <a href="/dashboard/steam/generate" class="nav-item"><span>⚡</span><span>Generate</span></a>
                <div class="nav-section">🎯 Ubisoft</div>
                <a href="/dashboard/ubisoft/games" class="nav-item"><span>🎮</span><span>Games</span></a>
                <a href="/dashboard/ubisoft/tokens" class="nav-item"><span>🎫</span><span>Tokens</span></a>
                <div class="nav-section">🔴 EA</div>
                <a href="/dashboard/ea/games" class="nav-item"><span>🎮</span><span>Games</span></a>
                <div class="nav-divider"></div>
                <a href="/dashboard/analytics" class="nav-item"><span>📈</span><span>Analytics</span></a>
                <a href="/dashboard/cooldowns" class="nav-item"><span>⏱️</span><span>Cooldowns</span></a>
            </nav>
            <% if (user) { %>
            <div class="sidebar-footer">
                <div><%= user.username %></div>
                <a href="/dashboard/logout" class="logout-btn">Logout</a>
            </div>
            <% } %>
        </aside>
        
        <main class="main-content">
            <header class="content-header">
                <h2>🎫 Steam Tokens</h2>
                <div class="action-bar">
                    <form method="POST" action="/dashboard/steam/tokens/reset-all" onsubmit="return confirm('Reset ALL tokens to available?')">
                        <button type="submit" class="btn btn-warning">Reset All Tokens</button>
                    </form>
                </div>
            </header>
            
            <div class="content-body">
                <div class="filter-bar">
                    <label>Select Game:</label>
                    <select class="form-control" onchange="location.href='/dashboard/steam/tokens?game=' + this.value" style="min-width: 300px">
                        <% games.forEach(g => { %>
                        <option value="<%= g.game_id %>" <%= selectedGame === g.game_id ? 'selected' : '' %>><%= g.game_name %></option>
                        <% }); %>
                    </select>
                </div>
                
                <div class="stats-row">
                    <div class="stat-box available"><div class="value"><%= stats.available %></div><div class="label">Available</div></div>
                    <div class="stat-box used"><div class="value"><%= stats.used %></div><div class="label">Used</div></div>
                    <div class="stat-box reserved"><div class="value"><%= stats.reserved %></div><div class="label">Reserved</div></div>
                    <div class="stat-box"><div class="value" style="color:#e94560"><%= stats.total %></div><div class="label">Total</div></div>
                </div>
                
                <div class="card">
                    <div class="card-title">Tokens for <%= selectedGame || 'No Game Selected' %></div>
                    <% if (tokens && tokens.length > 0) { %>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Account</th>
                                <th>Status</th>
                                <th>Used By</th>
                                <th>Regenerates</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% tokens.slice(0, 100).forEach(token => { %>
                            <tr>
                                <td>#<%= token.id %></td>
                                <td><%= token.account_id %></td>
                                <td><span class="badge badge-<%= token.status %>"><%= token.status %></span></td>
                                <td><%= token.used_by_username || '-' %></td>
                                <td><%= token.regenerates_at ? new Date(token.regenerates_at).toLocaleString() : '-' %></td>
                                <td>
                                    <form method="POST" action="/dashboard/steam/tokens/<%= token.id %>/delete" style="display:inline" onsubmit="return confirm('Delete?')">
                                        <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                                    </form>
                                </td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                    <% if (tokens.length > 100) { %>
                    <p style="text-align:center; color:#888; margin-top:15px">Showing first 100 of <%= tokens.length %> tokens</p>
                    <% } %>
                    <% } else { %>
                    <div class="empty-state">
                        <p>No tokens for this game yet.</p>
                    </div>
                    <% } %>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
