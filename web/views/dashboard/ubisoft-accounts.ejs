<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Pub's Lounge</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', sans-serif; background: #0a0a0f; color: #fff; min-height: 100vh; }
        .dashboard-container { display: flex; min-height: 100vh; }
        .sidebar { width: 260px; background: linear-gradient(180deg, #12121a 0%, #0d0d14 100%); border-right: 1px solid #1a1a2e; position: fixed; height: 100vh; overflow-y: auto; }
        .sidebar-header { padding: 20px; border-bottom: 1px solid #1a1a2e; display: flex; align-items: center; gap: 12px; }
        .sidebar-header h1 { font-size: 1.1rem; color: #e94560; }
        .sidebar-nav { padding: 15px 0; }
        .nav-section { padding: 8px 20px; font-size: 11px; text-transform: uppercase; color: #666; font-weight: 600; margin-top: 10px; }
        .nav-item { display: flex; align-items: center; gap: 12px; padding: 12px 20px; color: #888; text-decoration: none; border-left: 3px solid transparent; }
        .nav-item:hover { background: rgba(233, 69, 96, 0.1); color: #fff; }
        .nav-item.active { background: rgba(233, 69, 96, 0.15); color: #e94560; border-left-color: #e94560; }
        .nav-divider { height: 1px; background: #1a1a2e; margin: 10px 20px; }
        .sidebar-footer { padding: 15px 20px; border-top: 1px solid #1a1a2e; position: absolute; bottom: 0; width: 100%; background: #0d0d14; }
        .logout-btn { display: block; text-align: center; padding: 8px; background: rgba(220, 53, 69, 0.2); color: #dc3545; text-decoration: none; border-radius: 5px; margin-top: 10px; }
        .main-content { flex: 1; margin-left: 260px; }
        .content-header { padding: 20px 30px; background: #12121a; border-bottom: 1px solid #1a1a2e; display: flex; justify-content: space-between; align-items: center; }
        .content-body { padding: 25px 30px; }
        .card { background: #16213e; border: 1px solid #0f3460; border-radius: 10px; padding: 20px; }
        .table { width: 100%; border-collapse: collapse; }
        .table th, .table td { padding: 12px 15px; text-align: left; border-bottom: 1px solid #0f3460; }
        .table th { background: #0f3460; font-weight: 600; font-size: 0.85rem; }
        .table tr:hover { background: rgba(0, 112, 255, 0.05); }
        .btn { padding: 8px 16px; border: none; border-radius: 5px; cursor: pointer; text-decoration: none; font-size: 0.85rem; }
        .btn-primary { background: #0070ff; color: #fff; }
        .btn-secondary { background: #0f3460; color: #fff; }
        .btn-danger { background: #dc3545; color: #fff; }
        .btn-sm { padding: 5px 10px; font-size: 0.75rem; }
        .badge { padding: 4px 10px; border-radius: 4px; font-size: 0.75rem; font-weight: 600; }
        .badge-active { background: #28a745; color: #fff; }
        .badge-inactive { background: #6c757d; color: #fff; }
        .form-group { margin-bottom: 15px; }
        .form-group label { display: block; margin-bottom: 6px; color: #888; }
        .form-control { width: 100%; padding: 10px 12px; border: 1px solid #0f3460; border-radius: 6px; background: #1a1a2e; color: #fff; }
        .modal { display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); z-index: 1000; justify-content: center; align-items: center; }
        .modal.active { display: flex; }
        .modal-content { background: #16213e; border: 1px solid #0f3460; border-radius: 10px; padding: 25px; width: 90%; max-width: 500px; }
        .modal-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; padding-bottom: 15px; border-bottom: 1px solid #0f3460; }
        .modal-close { background: none; border: none; color: #888; font-size: 1.5rem; cursor: pointer; }
        .empty-state { text-align: center; padding: 40px; color: #666; }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <aside class="sidebar">
            <div class="sidebar-header"><span style="font-size:2rem">🍺</span><h1>Pub's Lounge</h1></div>
            <nav class="sidebar-nav">
                <a href="/dashboard" class="nav-item"><span>📊</span><span>Dashboard</span></a>
                <a href="/dashboard/tickets" class="nav-item"><span>🎫</span><span>Live Tickets</span></a>
                <div class="nav-section">🎮 Steam</div>
                <a href="/dashboard/steam/games" class="nav-item"><span>🎮</span><span>Games</span></a>
                <a href="/dashboard/steam/accounts" class="nav-item"><span>👤</span><span>Accounts</span></a>
                <div class="nav-section">🎯 Ubisoft</div>
                <a href="/dashboard/ubisoft/games" class="nav-item"><span>🎮</span><span>Games</span></a>
                <a href="/dashboard/ubisoft/accounts" class="nav-item active"><span>👤</span><span>Accounts</span></a>
                <a href="/dashboard/ubisoft/tokens" class="nav-item"><span>🎫</span><span>Tokens</span></a>
                <a href="/dashboard/ubisoft/generate" class="nav-item"><span>⚡</span><span>Generate</span></a>
                <div class="nav-section">🔴 EA</div>
                <a href="/dashboard/ea/games" class="nav-item"><span>🎮</span><span>Games</span></a>
                <div class="nav-divider"></div>
                <a href="/dashboard/analytics" class="nav-item"><span>📈</span><span>Analytics</span></a>
            </nav>
            <% if (user) { %>
            <div class="sidebar-footer"><div><%= user.username %></div><a href="/dashboard/logout" class="logout-btn">Logout</a></div>
            <% } %>
        </aside>
        
        <main class="main-content">
            <header class="content-header">
                <h2>👤 Ubisoft Accounts</h2>
                <button class="btn btn-primary" onclick="openModal('addModal')">+ Add Account</button>
            </header>
            
            <div class="content-body">
                <div class="card">
                    <% if (accounts && accounts.length > 0) { %>
                    <table class="table">
                        <thead><tr><th>Account #</th><th>Name</th><th>Email</th><th>Status</th><th>Actions</th></tr></thead>
                        <tbody>
                            <% accounts.forEach(acc => { %>
                            <tr>
                                <td><strong><%= acc.account_number %></strong></td>
                                <td><%= acc.account_name || '-' %></td>
                                <td><%= acc.email || '-' %></td>
                                <td><span class="badge badge-<%= acc.status === 'active' ? 'active' : 'inactive' %>"><%= acc.status %></span></td>
                                <td>
                                    <button class="btn btn-sm btn-secondary" onclick="openEditModal(<%= JSON.stringify(acc).replace(/"/g, '&quot;') %>)">Edit</button>
                                    <form method="POST" action="/dashboard/ubisoft/accounts/<%= acc.id %>/delete" style="display:inline" onsubmit="return confirm('Delete?')">
                                        <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                                    </form>
                                </td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                    <% } else { %>
                    <div class="empty-state"><p>No accounts yet.</p></div>
                    <% } %>
                </div>
            </div>
        </main>
    </div>
    
    <div class="modal" id="addModal">
        <div class="modal-content">
            <div class="modal-header"><h3>Add Ubisoft Account</h3><button class="modal-close" onclick="closeModal()">&times;</button></div>
            <form method="POST" action="/dashboard/ubisoft/accounts/add">
                <div class="form-group"><label>Account Number</label><input type="text" name="account_number" class="form-control" required></div>
                <div class="form-group"><label>Account Name</label><input type="text" name="account_name" class="form-control"></div>
                <div class="form-group"><label>Email</label><input type="email" name="email" class="form-control"></div>
                <button type="submit" class="btn btn-primary" style="width:100%">Add Account</button>
            </form>
        </div>
    </div>
    
    <div class="modal" id="editModal">
        <div class="modal-content">
            <div class="modal-header"><h3>Edit Account</h3><button class="modal-close" onclick="closeModal()">&times;</button></div>
            <form method="POST" id="editForm">
                <div class="form-group"><label>Account Number</label><input type="text" name="account_number" id="edit_account_number" class="form-control" required></div>
                <div class="form-group"><label>Account Name</label><input type="text" name="account_name" id="edit_account_name" class="form-control"></div>
                <div class="form-group"><label>Email</label><input type="email" name="email" id="edit_email" class="form-control"></div>
                <div class="form-group"><label>Status</label><select name="status" id="edit_status" class="form-control"><option value="active">Active</option><option value="inactive">Inactive</option></select></div>
                <button type="submit" class="btn btn-primary" style="width:100%">Save</button>
            </form>
        </div>
    </div>
    
    <script>
        function openModal(id) { document.getElementById(id).classList.add('active'); }
        function closeModal() { document.querySelectorAll('.modal').forEach(m => m.classList.remove('active')); }
        document.querySelectorAll('.modal').forEach(m => m.addEventListener('click', e => { if (e.target === m) closeModal(); }));
        function openEditModal(acc) {
            document.getElementById('editForm').action = '/dashboard/ubisoft/accounts/' + acc.id + '/edit';
            document.getElementById('edit_account_number').value = acc.account_number || '';
            document.getElementById('edit_account_name').value = acc.account_name || '';
            document.getElementById('edit_email').value = acc.email || '';
            document.getElementById('edit_status').value = acc.status || 'active';
            openModal('editModal');
        }
    </script>
</body>
</html>
