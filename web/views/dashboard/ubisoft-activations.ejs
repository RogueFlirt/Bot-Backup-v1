<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Pub's Lounge</title>
    <link rel="stylesheet" href="/dashboard/static/css/dashboard.css">
    <style>
        .nav-section-title {
            padding: 8px 16px;
            font-size: 11px;
            text-transform: uppercase;
            color: var(--text-muted);
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        .filter-bar {
            display: flex;
            gap: 12px;
            align-items: flex-end;
            margin-bottom: 20px;
            padding: 16px;
            background: var(--bg-secondary);
            border-radius: 8px;
            border: 1px solid var(--border-color);
        }
        .filter-bar .form-group {
            margin-bottom: 0;
        }
        .filter-bar label {
            display: block;
            margin-bottom: 6px;
            font-weight: 500;
            color: var(--text-muted);
            font-size: 12px;
        }
        .filter-bar select, .filter-bar input {
            padding: 8px 12px;
            background: var(--bg-tertiary);
            border: 1px solid var(--border-color);
            border-radius: 6px;
            color: var(--text-primary);
        }
        .pagination {
            display: flex;
            gap: 8px;
            justify-content: center;
            margin-top: 20px;
        }
        .pagination a, .pagination span {
            padding: 8px 12px;
            background: var(--bg-secondary);
            border: 1px solid var(--border-color);
            border-radius: 4px;
            color: var(--text-primary);
            text-decoration: none;
        }
        .pagination a:hover {
            background: var(--bg-tertiary);
        }
        .pagination .current {
            background: var(--accent-primary);
            border-color: var(--accent-primary);
        }
    </style>
</head>
<body>
    <div class="app-container">
        <aside class="sidebar">
            <div class="sidebar-header">
                <span class="logo">🍺</span>
                <h1>Pub's Lounge</h1>
            </div>
            <nav class="sidebar-nav">
                <a href="/dashboard" class="nav-item"><span class="nav-icon">📊</span><span>Dashboard</span></a>
                <a href="/dashboard/games" class="nav-item"><span class="nav-icon">🎮</span><span>Games</span></a>
                <a href="/dashboard/accounts" class="nav-item"><span class="nav-icon">👤</span><span>Accounts</span></a>
                <a href="/dashboard/tokens" class="nav-item"><span class="nav-icon">🎫</span><span>Tokens</span></a>
                <a href="/dashboard/transcripts" class="nav-item"><span class="nav-icon">📜</span><span>Transcripts</span></a>
                <div class="nav-divider"></div>
                <div class="nav-section-title">🎯 Ubisoft</div>
                <a href="/dashboard/ubisoft-games" class="nav-item"><span class="nav-icon">🎮</span><span>Ubisoft Games</span></a>
                <a href="/dashboard/ubisoft-accounts" class="nav-item"><span class="nav-icon">👤</span><span>Ubisoft Accounts</span></a>
                <a href="/dashboard/ubisoft-tokens" class="nav-item"><span class="nav-icon">🎫</span><span>Ubisoft Tokens</span></a>
                <a href="/dashboard/ubisoft-activations" class="nav-item active"><span class="nav-icon">✅</span><span>Ubisoft Activations</span></a>
                <div class="nav-divider"></div>
                <a href="/dashboard/analytics" class="nav-item"><span class="nav-icon">📈</span><span>Analytics</span></a>
                <a href="/dashboard/activations" class="nav-item"><span class="nav-icon">✅</span><span>Activated Users</span></a>
            </nav>
            <% if (user) { %>
            <div class="sidebar-footer">
                <div class="user-info">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="" class="user-avatar" onerror="this.style.display='none'">
                    <div class="user-details">
                        <span class="user-name"><%= user.username %></span>
                        <span class="user-role">Staff</span>
                    </div>
                </div>
                <a href="/dashboard/logout" class="logout-btn">Logout</a>
            </div>
            <% } %>
        </aside>
        
        <main class="main-content">
            <header class="content-header">
                <h2>🎯 <%= title %> (<%= pagination.total %>)</h2>
            </header>
            
            <div class="content-body">
                <!-- Filter Bar -->
                <form method="GET" class="filter-bar">
                    <div class="form-group">
                        <label for="gameId">Game</label>
                        <select id="gameId" name="gameId">
                            <option value="">All Games</option>
                            <% games.forEach(g => { %>
                            <option value="<%= g.id %>" <%= filters.gameId == g.id ? 'selected' : '' %>><%= g.game_name %></option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="username">Username</label>
                        <input type="text" id="username" name="username" value="<%= filters.username || '' %>" placeholder="Search...">
                    </div>
                    <button type="submit" class="btn btn-primary">Filter</button>
                    <a href="/dashboard/ubisoft-activations" class="btn btn-secondary">Clear</a>
                </form>
                
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Date</th>
                                <th>User</th>
                                <th>Game</th>
                                <th>Account</th>
                                <th>Status</th>
                                <th>Ticket</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (activations.length === 0) { %>
                            <tr>
                                <td colspan="7" style="text-align: center; padding: 40px; color: var(--text-muted);">
                                    No activations yet.
                                </td>
                            </tr>
                            <% } %>
                            <% activations.forEach(a => { %>
                            <tr>
                                <td>#<%= a.id %></td>
                                <td><%= new Date(a.activated_at).toLocaleString() %></td>
                                <td><strong><%= a.username %></strong></td>
                                <td><%= a.game_name || 'Unknown' %></td>
                                <td><%= a.account_name || 'Unknown' %></td>
                                <td>
                                    <% if (a.success) { %>
                                    <span class="badge badge-success">✅ Success</span>
                                    <% } else { %>
                                    <span class="badge badge-danger">❌ Failed</span>
                                    <% if (a.error_message) { %>
                                    <br><small style="color: var(--text-muted);"><%= a.error_message %></small>
                                    <% } %>
                                    <% } %>
                                </td>
                                <td>
                                    <% if (a.ticket_id) { %>
                                    <code><%= a.ticket_id %></code>
                                    <% } else { %>
                                    <span style="color: var(--text-muted);">—</span>
                                    <% } %>
                                </td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
                
                <!-- Pagination -->
                <% if (pagination.totalPages > 1) { %>
                <div class="pagination">
                    <% if (pagination.page > 1) { %>
                    <a href="?page=<%= pagination.page - 1 %>&gameId=<%= filters.gameId || '' %>&username=<%= filters.username || '' %>">← Prev</a>
                    <% } %>
                    
                    <% for (let i = 1; i <= pagination.totalPages; i++) { %>
                        <% if (i === pagination.page) { %>
                        <span class="current"><%= i %></span>
                        <% } else if (i <= 3 || i > pagination.totalPages - 3 || Math.abs(i - pagination.page) <= 2) { %>
                        <a href="?page=<%= i %>&gameId=<%= filters.gameId || '' %>&username=<%= filters.username || '' %>"><%= i %></a>
                        <% } else if (i === 4 || i === pagination.totalPages - 3) { %>
                        <span>...</span>
                        <% } %>
                    <% } %>
                    
                    <% if (pagination.page < pagination.totalPages) { %>
                    <a href="?page=<%= pagination.page + 1 %>&gameId=<%= filters.gameId || '' %>&username=<%= filters.username || '' %>">Next →</a>
                    <% } %>
                </div>
                <% } %>
            </div>
        </main>
    </div>
</body>
</html>
