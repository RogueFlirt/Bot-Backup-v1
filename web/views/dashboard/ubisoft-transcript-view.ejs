<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Pub's Lounge</title>
    <link rel="stylesheet" href="/dashboard/static/css/dashboard.css">
    <style>
        .nav-section-title {
            padding: 8px 16px;
            font-size: 11px;
            text-transform: uppercase;
            color: var(--text-muted);
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        .transcript-info {
            background: var(--bg-secondary);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
        }
        .transcript-info h3 {
            margin-bottom: 16px;
            font-size: 18px;
        }
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
        }
        .info-item {
            font-size: 14px;
        }
        .info-item strong {
            color: var(--text-muted);
            display: block;
            margin-bottom: 4px;
        }
        .messages-container {
            background: var(--bg-secondary);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 20px;
        }
        .messages-container h3 {
            margin-bottom: 16px;
            font-size: 18px;
        }
        .message {
            padding: 12px;
            margin-bottom: 12px;
            border-radius: 8px;
            background: var(--bg-tertiary);
        }
        .message.bot {
            border-left: 3px solid var(--accent-primary);
        }
        .message.staff {
            border-left: 3px solid var(--accent-success);
        }
        .message.user {
            border-left: 3px solid var(--text-muted);
        }
        .message-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            font-size: 13px;
        }
        .message-author {
            font-weight: 600;
        }
        .message-time {
            color: var(--text-muted);
        }
        .message-content {
            font-size: 14px;
            line-height: 1.5;
            white-space: pre-wrap;
            word-break: break-word;
        }
        .message-attachments {
            margin-top: 8px;
            font-size: 12px;
        }
        .message-attachments a {
            color: var(--accent-primary);
        }
        .empty-messages {
            text-align: center;
            padding: 40px;
            color: var(--text-muted);
        }
        .back-link {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            color: var(--text-muted);
            text-decoration: none;
            margin-bottom: 16px;
        }
        .back-link:hover {
            color: var(--text-primary);
        }
    </style>
</head>
<body>
    <div class="app-container">
        <aside class="sidebar">
            <div class="sidebar-header">
                <span class="logo">🍺</span>
                <h1>Pub's Lounge</h1>
            </div>
            <nav class="sidebar-nav">
                <a href="/dashboard" class="nav-item"><span class="nav-icon">📊</span><span>Dashboard</span></a>
                <a href="/dashboard/games" class="nav-item"><span class="nav-icon">🎮</span><span>Games</span></a>
                <a href="/dashboard/accounts" class="nav-item"><span class="nav-icon">👤</span><span>Accounts</span></a>
                <a href="/dashboard/tokens" class="nav-item"><span class="nav-icon">🎫</span><span>Tokens</span></a>
                <a href="/dashboard/transcripts" class="nav-item"><span class="nav-icon">📜</span><span>Transcripts</span></a>
                <div class="nav-divider"></div>
                <div class="nav-section-title">🎯 Ubisoft</div>
                <a href="/dashboard/ubisoft-games" class="nav-item"><span class="nav-icon">🎮</span><span>Ubisoft Games</span></a>
                <a href="/dashboard/ubisoft-accounts" class="nav-item"><span class="nav-icon">👤</span><span>Ubisoft Accounts</span></a>
                <a href="/dashboard/ubisoft-tokens" class="nav-item"><span class="nav-icon">🎫</span><span>Ubisoft Tokens</span></a>
                <a href="/dashboard/ubisoft-activations" class="nav-item"><span class="nav-icon">✅</span><span>Ubisoft Activations</span></a>
                <a href="/dashboard/ubisoft-transcripts" class="nav-item active"><span class="nav-icon">📜</span><span>Ubisoft Transcripts</span></a>
                <a href="/dashboard/ubisoft-generate" class="nav-item"><span class="nav-icon">⚡</span><span>Manual Generate</span></a>
                <div class="nav-divider"></div>
                <a href="/dashboard/analytics" class="nav-item"><span class="nav-icon">📈</span><span>Analytics</span></a>
                <a href="/dashboard/activations" class="nav-item"><span class="nav-icon">✅</span><span>Activated Users</span></a>
            </nav>
            <% if (user) { %>
            <div class="sidebar-footer">
                <div class="user-info">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="" class="user-avatar" onerror="this.style.display='none'">
                    <div class="user-details">
                        <span class="user-name"><%= user.username %></span>
                        <span class="user-role">Staff</span>
                    </div>
                </div>
                <a href="/dashboard/logout" class="logout-btn">Logout</a>
            </div>
            <% } %>
        </aside>
        
        <main class="main-content">
            <header class="content-header">
                <h2>🎯 <%= title %></h2>
            </header>
            
            <div class="content-body">
                <a href="/dashboard/ubisoft-transcripts" class="back-link">← Back to Transcripts</a>
                
                <div class="transcript-info">
                    <h3>Ticket Information</h3>
                    <div class="info-grid">
                        <div class="info-item">
                            <strong>Ticket ID</strong>
                            <%= transcript.ticket_id %>
                        </div>
                        <div class="info-item">
                            <strong>User</strong>
                            <%= transcript.username || 'Unknown' %> (<%= transcript.user_id || 'N/A' %>)
                        </div>
                        <div class="info-item">
                            <strong>Game</strong>
                            <%= transcript.game_name || 'N/A' %>
                        </div>
                        <div class="info-item">
                            <strong>Close Reason</strong>
                            <%= transcript.close_reason || 'N/A' %>
                        </div>
                        <div class="info-item">
                            <strong>Created</strong>
                            <%= transcript.created_at ? new Date(transcript.created_at).toLocaleString() : 'N/A' %>
                        </div>
                        <div class="info-item">
                            <strong>Closed</strong>
                            <%= transcript.closed_at ? new Date(transcript.closed_at).toLocaleString() : 'N/A' %>
                        </div>
                    </div>
                </div>
                
                <div class="messages-container">
                    <h3>Messages (<%= messages ? messages.length : 0 %>)</h3>
                    
                    <% if (messages && messages.length > 0) { %>
                    <div class="messages-list">
                        <% messages.forEach(msg => { %>
                        <div class="message <%= msg.isBot ? 'bot' : (msg.isStaff ? 'staff' : 'user') %>">
                            <div class="message-header">
                                <span class="message-author">
                                    <%= msg.author || 'Unknown' %>
                                    <% if (msg.isBot) { %> 🤖<% } %>
                                    <% if (msg.isStaff) { %> 👮<% } %>
                                </span>
                                <span class="message-time">
                                    <%= msg.timestamp ? new Date(msg.timestamp).toLocaleString() : '' %>
                                </span>
                            </div>
                            <div class="message-content"><%= msg.content || '[No content]' %></div>
                            <% if (msg.attachments && msg.attachments.length > 0) { %>
                            <div class="message-attachments">
                                📎 Attachments: 
                                <% msg.attachments.forEach((att, i) => { %>
                                <a href="<%= att.url %>" target="_blank"><%= att.name %></a><%= i < msg.attachments.length - 1 ? ', ' : '' %>
                                <% }) %>
                            </div>
                            <% } %>
                        </div>
                        <% }) %>
                    </div>
                    <% } else { %>
                    <div class="empty-messages">
                        <p>No messages recorded for this transcript.</p>
                    </div>
                    <% } %>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
